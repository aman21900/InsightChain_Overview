# InsightLoop
An LLM-powered Natural Language BI Dashboard that converts plain English questions into SQL queries, executes them, generates Plotly visualizations, and renders results in a Gradio interface.
